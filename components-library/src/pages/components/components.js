import Sidebar from "components/navigation/sidebar";

const Components = () => {
  return (
    <div className="main">
        <Sidebar/>
        {/* added the details for the component. */}
    </div>
  );
};

export default Components;
